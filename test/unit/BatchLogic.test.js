const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("BatchLogic", function () {
    // tested via UtilitiesV2
    it("Should act as a placeholder for specific BatchLogic tests", function () {
        expect(true).to.be.true;
    });
});
