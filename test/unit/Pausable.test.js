const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Pausable", function () {
    // tested via UtilitiesV2
    it("Should act as a placeholder for specific Pausable tests", function () {
        expect(true).to.be.true;
    });
});
