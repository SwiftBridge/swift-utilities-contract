const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("TokenHelper", function () {
    // tested via UtilitiesV2
    it("Should act as a placeholder for specific TokenHelper tests", function () {
        expect(true).to.be.true;
    });
});
